function populateList(input) {
    //write create and populate list elements here
}

document.addEventListener('DOMContentLoaded', () => {
    fetch('./output.json')
      //pass data to function here
});